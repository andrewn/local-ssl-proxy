#!/usr/bin/env node

var commandline = require("../dist/bundle").commandline;
var proxy = require("../dist/bundle").proxy;

var args = commandline.parse();

var config = args.config || { proxy: args };

proxy(config, args);
